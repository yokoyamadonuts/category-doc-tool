# Document Without Frontmatter

This document has no YAML frontmatter. CatDoc should:
1. Use the filename as the id
2. Use the first H1 as the title
3. Use the default domain

## Some Content

Content without explicit metadata.
